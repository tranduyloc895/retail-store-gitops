apiVersion: monitoring.coreos.com/v1
kind: ServiceMonitor
metadata:
  name: catalog
  namespace: retail-store
  labels:
    app: catalog
    release: kps
spec:
  selector:
    matchLabels:
      app: catalog
  endpoints:
    - port: http
      path: /metrics
      interval: 30s
      scrapeTimeout: 10s