apiVersion: v1
kind: Service
metadata:
  name: checkout
  namespace: retail-store
  labels:
    app: checkout
spec:
  type: ClusterIP
  selector:
    app: checkout
  ports:
    - name: http
      port: 80
      targetPort: 8080
      protocol: TCP
