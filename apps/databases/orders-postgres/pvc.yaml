# Volume lưu dữ liệu PostgreSQL (gp3 default StorageClass, mã hóa at-rest).
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: orders-postgres-data
  namespace: retail-store
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 5Gi
