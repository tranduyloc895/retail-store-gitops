apiVersion: monitoring.coreos.com/v1
kind: ServiceMonitor
metadata:
  name: orders
  namespace: retail-store
  labels:
    app: orders
    release: kps
spec:
  selector:
    matchLabels:
      app: orders
  endpoints:
    - port: http
      path: /actuator/prometheus
      interval: 30s
      scrapeTimeout: 10s