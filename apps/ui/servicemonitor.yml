apiVersion: monitoring.coreos.com/v1
kind: ServiceMonitor
metadata:
  name: ui
  namespace: retail-store
  labels:
    app: ui
    release: kps
spec:
  selector:
    matchLabels:
      app: ui
  endpoints:
    - port: http
      path: /actuator/prometheus
      interval: 30s
      scrapeTimeout: 10s