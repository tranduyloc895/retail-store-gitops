apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
# Dev overlay — single environment, no patches yet.
# To add per-env overrides (e.g. replicas: 1 for cost saving), add patches here.
# Example:
#   patches:
#     - target:
#         kind: Deployment
#         name: ui
#       patch: |-
#         - op: replace
#           path: /spec/replicas
#           value: 1
